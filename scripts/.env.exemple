# ===============================================================
#     Arquivo de Configuração para os Scripts de Implantação
# ===============================================================
#
# INSTRUÇÕES:
# 1. Copie este arquivo para ".env" (cp .env.example .env)
# 2. Edite o arquivo .env com suas informações.
#
# ===============================================================

# -- CHAVE DE AUTENTICAÇÃO DO TAILSCALE --
# Gere esta chave no painel do Tailscale (reutilizável e pré-autorizada).
TAILSCALE_AUTH_KEY="tskey-auth-SUA-CHAVE-DE-AUTENTICACAO-AQUI"

# -- ENDEREÇO DO SERVIDOR ZABBIX --
# O IP fixo da VPN Tailscale do seu servidor Zabbix.
ZABBIX_SERVER_IP="IP-DO-SERVIDOR"